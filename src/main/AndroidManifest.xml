<?xml version="1.0" encoding="utf-8"?>
<!--
  Library manifest. AGP merges this into the host app.
  - INTERNET / ACCESS_NETWORK_STATE: device REST calls (verify, entitlements, events, resolve).
  - com.android.vending.BILLING is contributed automatically by billing-ktx's manifest.
  - PaywallActivity is exported=false: it is only ever started by the SDK itself.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <activity
            android:name="com.cashsdk.paywall.PaywallActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.CashSDK.Paywall" />
    </application>

</manifest>
