<?xml version="1.0" encoding="utf-8"?>
<!--
  Theme for PaywallActivity. Translucent + no action bar so the paywall floats over
  the host UI (Compose draws its own scrim/background per config.presentation). Uses a
  framework parent to avoid pulling in the Material Components (XML) dependency — the
  paywall UI itself is 100% Jetpack Compose Material 3.
-->
<resources>
    <style name="Theme.CashSDK.Paywall" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Activity</item>
    </style>
</resources>
